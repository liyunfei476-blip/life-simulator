{
  "name": "crossing-life-simulator",
  "version": "0.3.0",
  "private": true,
  "description": "Browser-based text adventure, ready for static hosting",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  }
}
